import { HeartState } from "./heart.types";

const HEART_INITIAL_STATE: HeartState = {
	heartRates: [],
	currentHeartRate: null,
	currentCoherence: null,
	maxCoherence: null,
};

export default HEART_INITIAL_STATE;
